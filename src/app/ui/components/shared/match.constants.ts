/** Cantidad total de tarjetas mostradas en el tablero (6 Galatea + 6 tema elegido, A-009). */
export const TOTAL_BOARD_CARDS = 12;

/** Máximo de preguntas que el jugador puede responder por partida (FR-009). */
export const MAX_ANSWERABLE_CARDS = 6;

/** Puntos otorgados por cada respuesta correcta. */
export const POINTS_PER_CORRECT_ANSWER = 10;
